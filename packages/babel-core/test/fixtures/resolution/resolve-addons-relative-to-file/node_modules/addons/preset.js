module.exports = {
  plugins: [plugin],
};

function plugin () {
  return {
    visitor: {
      StringLiteral: function (path) {
        path.node.value =
          path.node.value.toUpperCase();
      },
    },
  };
}
