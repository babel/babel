let B = babelHelpers.decorate([dec], function (_initialize, _A) {
  "use strict";

  class B extends _A {
    constructor() {
      const foo = () => {
        super();

        _initialize(this);
      };

      if (a) {
        _initialize(this);

        super();
      } else {
        foo();
      }

      while (0) {
        _initialize(this);

        super();
      }

      _initialize(this);

      super();
    }

  }

  return {
    F: B,
    d: []
  };
}, A);
