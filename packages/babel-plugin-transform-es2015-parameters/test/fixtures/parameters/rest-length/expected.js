var t = function (f, ..._ref) {
  let [...items] = [..._ref];

  items[0];
  items[items.length - 1];
};

function t(f, ..._ref2) {
  let [...items] = [..._ref2];

  items;
  items[0];
  items[items.length - 1];
}