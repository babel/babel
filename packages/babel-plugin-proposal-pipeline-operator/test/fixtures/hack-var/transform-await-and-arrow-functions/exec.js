const triple = (x) => x * 3;

async function myFunction(n) {
  return n
    |> Math.abs(var)
    |> Promise.resolve(var)
    |> await var
    |> triple(var);
}

return myFunction(-7).then(result => {
  expect(result).toBe(21);
});
