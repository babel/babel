const result = 5
  |> Math.pow(var, 2)
  |> [1, 2, 3].map(n => n + var
    |> var * 2
    |> `${var} apples`
    |> var.toUpperCase())
  |> var.join();

expect(result).toEqual('52 APPLES,54 APPLES,56 APPLES');
