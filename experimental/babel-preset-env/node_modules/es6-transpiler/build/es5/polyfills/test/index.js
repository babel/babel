var reporter = (require('nodeunit').reporters).default
	, path = require('path');

reporter.run(
	['specs/RegExp'].map( function(moduleName)  {return path.join(__dirname, (("" + moduleName) + ".js"))} )
);
